# SLDX_Ambition_Website 编辑指南

## 📖 前言

本指南面向没有编程基础的团队成员，教你如何更新战队网站内容。

**重要说明**：
- 所有修改都需要通过 **Pull Request (PR)** 提交给管理员审核
- 审核通过后会由管理员合并到主分支，网站会自动更新
- 不要直接修改主分支，需要通过 PR 流程

---

## 🚀 快速开始（三种方式）

### 方式一：在线编辑（最简单，推荐新手）

适合快速修改文字内容，无需安装任何软件。

#### 步骤：

1. **打开要编辑的页面**
   - 访问网站：https://www.sldx-ambition.top
   - 找到要编辑的页面

2. **点击"在 GitHub 上编辑"按钮**
   - 在页面底部找到编辑按钮
   - 首次使用会提示你 Fork 仓库到个人账号

3. **编辑内容**
   - 在 GitHub 编辑器中修改内容
   - 支持 Markdown 语法（见下方教程）

4. **提交更改**
   - 滚动到页面底部
   - 填写修改说明（必填）
   - 选择 **"Create a new branch"**
   - 点击 **"Propose changes"**

5. **创建 Pull Request**
   - 点击 **"Create pull request"**
   - 等待管理员审核
   - 审核通过并合并后，网站会自动更新（约 1-2 分钟）

### 方式二：使用 VS Code 编辑（推荐）

适合大量修改或批量更新。

#### 1. 安装必要软件

- **VS Code**：https://code.visualstudio.com/
- **Git**：https://git-scm.com/zh-cn
- 安装 VS Code 插件：`Markdown Preview Enhanced`

#### 2. Fork 仓库

1. 打开 https://github.com/SYLU-Ambition/SLDX_Ambition_Website
2. 点击右上角 **Fork** 按钮
3. 复制你的仓库地址

#### 3. 克隆仓库到本地

```bash
# 选择一个文件夹
cd ~/Documents  # Mac/Linux
# 或
cd D:\Projects  # Windows

# 克隆你的 fork 仓库
git clone https://github.com/你的用户名/SLDX_Ambition_Website.git

# 进入项目目录
cd SLDX_Ambition_Website
```

#### 4. 编辑内容

```bash
# 用 VS Code 打开项目
code .
```

- 所有页面在 `doc/` 目录下
- 编辑 `.md` 文件
- 按 `Ctrl+Shift+V` 预览效果

#### 5. 在 VS Code 中提交并推送

在 VS Code 左侧点击 **源代码管理** 图标（或按 `Ctrl+Shift+G`）：

1. 点击文件名旁边的 **+** 号（暂存更改）
2. 在输入框填写修改说明（如 "更新成员介绍"）
3. 点击 **提交** 按钮（✓）
4. 点击 **···** → **推送** 按钮（或按 `Ctrl+Shift+P` 输入 "Git: Push"）

或者使用终端：

```bash
# 添加修改
git add .

# 提交更改
git commit -m "修改说明：更新了 XX 内容"

# 推送到你的仓库
git push
```

#### 6. 在 GitHub 上创建 Pull Request

推送完成后，在浏览器中操作：

1. 打开你的仓库页面：`https://github.com/你的用户名/SLDX_Ambition_Website`
2. 如果有刚推送的提交，页面顶部会显示提示，点击 **"Compare & pull request"**
3. 或者点击顶部菜单的 **Pull requests** → **New pull request**
4. 选择 base 仓库为 `SYLU-Ambition/SLDX_Ambition_Website:main`
5. 填写 PR 说明，描述你修改了什么内容
6. 点击 **"Create pull request"**



## 📝 Markdown 基础语法

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 文字格式

```markdown
**加粗**  *斜体*  ~~删除线~~
```

### 列表

```markdown
- 无序列表 1
- 无序列表 2

1. 有序列表 1
2. 有序列表 2
```

### 链接

```markdown
[文字](https://链接地址)
```

### 图片

```markdown
![图片说明](/images/photo.jpg)
```

### 代码块

````markdown
```python
print("Hello World")
```
````

### 引用

```markdown
> 这是一段引用
```

### 表格

```markdown
| 姓名 | 年龄 | 组别 |
|------|------|------|
| 张三 | 20   | 嵌入式 |
| 李四 | 21   | 视觉  |
```

---

## 🔄 Pull Request (PR) 完整流程

### 什么是 Pull Request？

Pull Request 是你向项目提出"我想修改这些内容，请审核并合并"的请求。

### 完整流程：

```
1. Fork 仓库 → 复制一份到你的账号
2. 修改内容 → 编辑文件或代码
3. 提交更改 → 保存到你的仓库
4. 创建 PR → 请求管理员审核
5. 等待审核 → 管理员检查修改
6. 合并 PR → 修改被接受，网站自动更新
```

### PR 被拒绝怎么办？

- 管理员可能会提出修改建议
- 根据评论修改后重新提交即可
- 这是正常的协作流程，不要担心

---

## 📁 文件结构说明

```
SLDX_Ambition_Website/
├── doc/                    # 所有文档都在这里
│   ├── index.md           # 首页
│   ├── Ambition_Introduction/  # 战队介绍
│   ├── History/           # 历史
│   ├── Sign_Up/           # 招新
│   ├── Photo/             # 照片展示
│   └── Knowledge/         # 知识库
├── .vitepress/            # 网站配置
├── public/                # 图片等静态资源
└── README.md              # 项目说明
```

---

## 🖼️ 上传图片

网站的图片统一放在 `doc/public/` 目录下（VitePress 会把 `doc/` 下的 `public/` 当作静态资源根目录），在 Markdown 中用**以 `/` 开头的绝对路径**引用。

### 步骤：

1. 把图片放到 `doc/public/` 下对应的分类目录，例如：
   - 照片图库：`doc/public/Photo/`
   - 战队介绍相关图：`doc/public/Ambition_Introduction/`
   - 历史：`doc/public/History/`
   - 知识库：`doc/public/Knowledge/`

2. 在 Markdown 中引用（注意路径以 `/` 开头，不写 `public`）：
   ```markdown
   ![图片说明](/Photo/photo_23dks/01.jpg)
   ![机器人图](/Ambition_Introduction/robot_photo/19BB.png)
   ```

### 📏 图片处理建议（重要）

**1. 图片必须压缩后再上传**
- 单张图片建议控制在 **1MB 以内**，优先用 `JPG`（照片）或 `WebP`（更小体积）。
- 相机/手机拍的原图动辄几 MB，**必须压缩后再放进 `doc/public/`**，不要直接传原图上来。
- 可用在线工具或本地命令压缩（如 `Squoosh`、`ImageMagick`、`TinyPNG`、`pngquant`）。

**2. 千万不要把「原图备份」这类大目录提交进仓库**
- `doc/public原图备份/`（及任何本地备份/网盘同步目录）**只保留在本地，不要 commit**。
- `.gitignore` 已经忽略了它们，**不要用 `git add -f` 强行加进去**。
- 这是血的教训：之前这个备份目录被误提交进历史，导致仓库膨胀到 7GB+，GitHub 直接拒绝接收推送。大目录一旦进历史，很难干净移除，还会把整个仓库拖垮。

**3. 图片命名**
- 用英文或拼音命名，不要用中文和空格。
- 建议带上分类和期数，容易管理，如 `photo_23dks_01.jpg`。

**4. 图片引用路径要写对**
- 路径以 `/` 开头，**不要**写成 `public/...` 或 `../public/...`。
- 引用前先在 `pnpm dev` 预览确认图片能正常显示。

### 注意：
- 团队合照建议说明时间、事件
- 大图片不仅影响加载速度，还会让仓库越滚越大，请务必压缩

---

## 📋 添加新页面

### 步骤：

1. 在 `doc/` 目录下新建 `.md` 文件
2. 添加页面头部：
   ```markdown
   ---
   title: 页面标题
   description: 页面描述
   ---

   # 页面标题

   正文内容...
   ```
3. 在侧边栏添加链接（需要管理员操作）

---

## ❓ 常见问题

### Q: 修改后网站为什么没有更新？

A: 需要等待管理员审核并合并 Pull Request，合并后约 1-2 分钟自动更新。

### Q: 如何查看我的 PR 状态？

A: 在 GitHub 仓库的 **Pull requests** 标签页查看。

### Q: 多人同时修改会冲突吗？

A: 如果修改不同文件不会冲突。如果修改同一文件，后提交的人需要解决冲突。

### Q: 如何撤销已提交的 PR？

A: 在你的 PR 页面底部点击 **"Close pull request"**。

### Q: 可以在本地预览效果吗？

A: 可以，需要安装 Node.js 和 pnpm：
```bash
pnpm install
pnpm dev
```

---

## 📞 需要帮助？

- 查看现有 PR：https://github.com/SYLU-Ambition/SLDX_Ambition_Website/pulls
- 提出 Issue：https://github.com/SYLU-Ambition/SLDX_Ambition_Website/issues
- 联系管理员：@Zhan_Kong

---

**感谢你为战队网站做出的贡献！** 🎉
